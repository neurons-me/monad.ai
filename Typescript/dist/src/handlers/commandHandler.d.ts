import type express from "express";
export declare const meCommandHandler: express.RequestHandler;
export declare const rootCompatHandler: express.RequestHandler;
export declare const rootCommandHandler: express.RequestHandler;
