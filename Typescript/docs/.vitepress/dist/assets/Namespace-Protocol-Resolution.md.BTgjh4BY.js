import{_ as a,o as s,c as t,ae as n}from"./chunks/framework.C7qU-Vik.js";const m=JSON.parse('{"title":"Namespace Resolution Protocol v0.2.1","description":"","frontmatter":{},"headers":[],"relativePath":"Namespace-Protocol-Resolution.md","filePath":"Namespace-Protocol-Resolution.md"}'),i={name:"Namespace-Protocol-Resolution.md"};function o(l,e,r,p,d,h){return s(),t("div",null,[...e[0]||(e[0]=[n(`<h1 id="namespace-resolution-protocol-v0-2-1" tabindex="-1">Namespace Resolution Protocol v0.2.1 <a class="header-anchor" href="#namespace-resolution-protocol-v0-2-1" aria-label="Permalink to &quot;Namespace Resolution Protocol v0.2.1&quot;">​</a></h1><p><strong>neurons.me / suiGn</strong><strong>Status:</strong> Implemented mesh draft through monad.ai Phase 6 <strong>License:</strong> CC0 1.0 Universal — Public Domain</p><hr><h2 id="current-implementation-snapshot" tabindex="-1">Current Implementation Snapshot <a class="header-anchor" href="#current-implementation-snapshot" aria-label="Permalink to &quot;Current Implementation Snapshot&quot;">​</a></h2><p>This document is the protocol-level spec. The <code>monad.ai</code> package currently implements the local HTTP/mesh binding through Phase 6:</p><ul><li><code>GET /.mesh/monads</code> lists local-kernel plus CLI-known monads.</li><li><code>GET /.mesh/resolve?namespace=...</code> returns namespace claimants.</li><li><code>GET /.mesh/resolve?monad=...</code> returns a named monad.</li><li><code>GET /resolve?target=me://namespace:read/path</code> bridges an NRP target.</li><li><code>[device:...]</code>, <code>[tag:...]</code>, and <code>[host:...]</code> selectors constrain mesh candidates.</li><li><code>computeScoreDetailed</code> selects among eligible monads and explains the score.</li><li><code>DecisionEntry</code> JSONL logs correlate decisions with outcome, latency, and continuous reward when <code>MONAD_DECISION_LOG</code> is set.</li></ul><p>See also:</p><ul><li><a href="./NRP/status"><code>NRP/status.md</code></a> for implementation status.</li><li><a href="./NRP/scoring"><code>NRP/scoring.md</code></a> for the production scoring contract.</li><li><a href="./NRP/testing"><code>NRP/testing.md</code></a> for the regression suite taxonomy.</li></ul><h2 id="preamble" tabindex="-1">Preamble <a class="header-anchor" href="#preamble" aria-label="Permalink to &quot;Preamble&quot;">​</a></h2><p>This document specifies the <strong>Namespace Resolution Protocol (NRP)</strong> for the <code>me://</code> URI scheme.</p><p>The NRP defines how a <code>me://</code> URI is resolved from a symbolic address into a concrete semantic value, across a distributed mesh of <strong>monads</strong> — without a central registry, without a central server, and without requiring persistent connectivity.</p><p>The canonical user-facing address is semantic:</p><div class="language-txt vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">txt</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>me://jabellae.cleaker.me/photos/iphone</span></span></code></pre></div><p>The user names meaning. The system chooses execution. It is the protocol that closes the gap between:</p><ul><li><p><strong>Semantic resolution</strong> — already implemented in <code>this.me</code>: local, mathematical, derivation-based. The kernel resolves <code>me.get(&quot;wallet.balance&quot;)</code> entirely offline.</p></li><li><p><strong>Topological resolution</strong> — the new work: how a requesting monad invisibly finds the best execution route for the target namespace, path, and key material.</p><p>These two layers are separate concerns, implemented separately, but always composed in that order:</p></li></ul><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>me://jabellae.cleaker.me/photos/iphone</span></span>
<span class="line"><span>         │                         │</span></span>
<span class="line"><span>    topological                 semantic</span></span>
<span class="line"><span>    (find namespace,            (resolve the</span></span>
<span class="line"><span>     choose monad route)         path locally)</span></span></code></pre></div><p>monads are invisible execution routes. A user does not need to know which monad serves <code>photos/iphone</code>; the namespace resolver decides that from index state, freshness, permissions, capabilities, budgets, and endpoint reachability.</p><p>Technical selectors may still force or constrain execution for diagnostics, debugging, or advanced routing:</p><div class="language-txt vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">txt</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>me://jabellae.cleaker.me[monadlisa]/photos/iphone</span></span></code></pre></div><p>This still targets the same semantic path, <code>jabellae.cleaker.me/photos/iphone</code>. The selector changes execution, not meaning.</p><p>The <strong>NRP</strong> specifies the topological layer. The semantic layer is already specified by <code>this.me</code>. <em>Meaning lives in the namespace. Execution lives in the mesh.</em></p><hr><h2 id="_0-sacred-rules" tabindex="-1">0. Sacred Rules <a class="header-anchor" href="#_0-sacred-rules" aria-label="Permalink to &quot;0. Sacred Rules&quot;">​</a></h2><h3 id="rule-1-—-ports-and-hosts-do-not-change-the-namespace" tabindex="-1">Rule 1 — Ports and hosts do not change the namespace <a class="header-anchor" href="#rule-1-—-ports-and-hosts-do-not-change-the-namespace" aria-label="Permalink to &quot;Rule 1 — Ports and hosts do not change the namespace&quot;">​</a></h3><p>The port never changes the namespace. The <em>physical host</em> is not the identity. The <strong>monad</strong> is the active execution agent.</p><div class="language-txt vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">txt</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>jabellae.cleaker.me              -&gt; namespace</span></span>
<span class="line"><span>monadlisa@127.0.0.1:8161         -&gt; monad instance + endpoint</span></span>
<span class="line"><span>http://127.0.0.1:8161            -&gt; transport only</span></span></code></pre></div><h3 id="rule-2-—-monads-are-invisible-execution-routes" tabindex="-1">Rule 2 — monads are invisible execution routes <a class="header-anchor" href="#rule-2-—-monads-are-invisible-execution-routes" aria-label="Permalink to &quot;Rule 2 — monads are invisible execution routes&quot;">​</a></h3><p>The <strong>namespace</strong> path is the meaning. The user thinks in semantics. The system resolves execution.</p><p>The default form is:</p><div class="language-txt vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">txt</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>me://jabellae.cleaker.me/profile</span></span></code></pre></div><p>An explicit <strong>monad</strong> selector is only a technical route override:</p><div class="language-txt vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">txt</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>me://jabellae.cleaker.me[monadlisa]/profile</span></span>
<span class="line"><span>me://jabellae.cleaker.me[monadluis]/profile</span></span></code></pre></div><p>Both point at the same semantic node:</p><div class="language-txt vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">txt</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>jabellae.cleaker.me/profile</span></span></code></pre></div><p><strong>The selector only changes the execution route:</strong> which <strong>monad</strong> is asked, with which freshness, budget, authority, capabilities, and operational state. If both monads are authorized and synchronized, their answers must converge.</p><p>No selector is the canonical public form. The resolver may internally use one <strong>monad</strong>, many <strong>monads</strong>, or the Total <strong>monad</strong> synthesis.</p><hr><h2 id="_1-definitions" tabindex="-1">1. Definitions <a class="header-anchor" href="#_1-definitions" aria-label="Permalink to &quot;1. Definitions&quot;">​</a></h2><p><strong>Namespace</strong> — A named semantic domain. Represented as a human-readable label (e.g., <code>jabellae.cleaker.me</code>). A namespace is owned by whoever holds its root key material. There is no central authority that grants or revokes a namespace.</p><p><strong>monad</strong> — A physical or logical runtime agent that can hold or reach a <code>.me</code> kernel instance and participate in the mesh. A monad can run on a laptop, phone, VM, server process, browser tab, or any other host, but the host is only placement metadata. The monad is the execution agent.</p><p>monads are <strong>not</strong> namespaces. A monad is a runtime execution context <strong>within</strong> a namespace — a place where the namespace can run code, access resources, consume budget, and serve semantic paths. A laptop monad, an iPhone monad, and a VM monad may all serve the <strong>same</strong> namespace. The namespace holds the meaning; the monad lends execution.</p><p><strong>Surface</strong> — Historical/compatibility term for a monad runtime. Older documents and APIs may say <code>surface</code>; this version treats <code>surface</code> as a legacy alias for <code>monad</code>.</p><p><strong>monad identity</strong> — The cryptographic identifier of a monad within a namespace:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>monad_id = &quot;monad:&quot; + sha256(monad_public_key)</span></span></code></pre></div><p>This is never the cleartext <code>monad_name</code>, and it is never the port. The <code>monad_name</code> is human. The endpoint is transport. The <code>monad_id</code> is continuity: whoever can sign as the matching private key is the same monad, even if it moves ports, hosts, or networks.</p><p><strong>cleaker(monad)</strong> — The signed continuity proof for a monad. The private key never leaves the monad host. The public key and signature may be published on <code>/__surface</code> so a resolver can confirm that an endpoint is still the same monad.</p><p><strong>Namespace key</strong> — The root secret from which all key material in a namespace is derived. Equivalent to the seed in the derivation-based identity model. Never transmitted; always local.</p><p><strong>Endpoint descriptor</strong> — A transport-layer locator for a monad (IP:port, NetGet placement, relay address, onion address, etc.). Stored encrypted inside the monad index. Not the monad&#39;s identity — just how to reach it right now.</p><p><strong>monad index</strong> — A <code>.me</code> secret space, scoped under the namespace key, that maps monad identities to their current endpoint descriptors, placement targets, budgets, capabilities, and status. It is the mesh&#39;s contact book and process table. Every authorized monad that holds the namespace key can read and write it.</p><p><strong>Total monad</strong> — The reduced synthesis of all active monads authorized for a namespace. It is the default coherent execution body behind ordinary <code>namespace/path</code> resolution when the mesh is available. It is not a list response and not merely parallel querying; parallel querying is only one possible internal strategy. The caller receives one coherent disclosure envelope.</p><p><strong>Claim token</strong> — A one-time, time-limited token that authorizes a new monad to join a namespace. Generated by an existing monad. Expires. Consumed on first use.</p><p><strong>Stealth root</strong> — A secret scope root that returns <code>undefined</code> on resolution. This is an honest absence, not an error. The NRP must preserve this distinction at the network level.</p><p><strong>Semantic island</strong> — A self-contained <code>.me</code> kernel instance holding a fragment of a namespace. Islands do not need to be constantly connected. They interact by exchanging mathematical hashes, not cleartext data.</p><hr><h2 id="_2-the-two-layer-model" tabindex="-1">2. The Two-Layer Model <a class="header-anchor" href="#_2-the-two-layer-model" aria-label="Permalink to &quot;2. The Two-Layer Model&quot;">​</a></h2><p>Resolution of a <code>me://</code> URI proceeds in two phases, always in this order.</p><h3 id="phase-1-—-topological-resolution" tabindex="-1">Phase 1 — Topological resolution <a class="header-anchor" href="#phase-1-—-topological-resolution" aria-label="Permalink to &quot;Phase 1 — Topological resolution&quot;">​</a></h3><p><strong>Input:</strong> <code>namespace</code> + <code>path</code> + optional technical selector <strong>Output:</strong> a reachable monad endpoint, a chosen set of monad endpoints, or an internal Total monad execution body.</p><p>The requesting monad must find an execution route that can serve the requested semantic path. This involves:</p><ol><li><p>Treating <code>namespace/path</code> as the canonical meaning.</p></li><li><p>Choosing a monad route internally from the monad index.</p></li><li><p>If a technical selector is present, constraining the route to the requested monads or monads set.</p></li><li><p>Looking up endpoint descriptors for the chosen <code>monad_id</code> values.</p></li><li><p>Establishing a connection to the endpoint or endpoints.</p><p>If the monad index does not contain the target <code>monad_id</code>, or if the target monad is unreachable, topological resolution fails. The request does not proceed to Phase 2.</p></li></ol><h3 id="phase-2-—-semantic-resolution" tabindex="-1">Phase 2 — Semantic resolution <a class="header-anchor" href="#phase-2-—-semantic-resolution" aria-label="Permalink to &quot;Phase 2 — Semantic resolution&quot;">​</a></h3><p><strong>Input:</strong> <code>path</code> + key material (if the path is within a secret scope) <strong>Output:</strong> the resolved value, or <code>undefined</code>, or a closed failure</p><p>Once the requesting monad has a connection to the chosen monad route, it sends a read request for the path. The target monad resolves the path locally using its <code>.me</code> kernel, or participates in internal Total monad synthesis, and returns a <strong>disclosure envelope</strong> (see Section 6).</p><p>Semantic resolution is local to the selected execution route. The requesting monad never sees another monad&#39;s kernel state directly — only the disclosure envelope for the specific path requested.</p><hr><h2 id="_3-monad-identity-and-the-monad-index" tabindex="-1">3. monad Identity and the monad Index <a class="header-anchor" href="#_3-monad-identity-and-the-monad-index" aria-label="Permalink to &quot;3. monad Identity and the monad Index&quot;">​</a></h2><h3 id="_3-1-monad-identity-derivation" tabindex="-1">3.1 monad identity derivation <a class="header-anchor" href="#_3-1-monad-identity-derivation" aria-label="Permalink to &quot;3.1 monad identity derivation&quot;">​</a></h3><p>A monad&#39;s identity is derived from its public key:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>monad_id = &quot;monad:&quot; + SHA-256(monad_public_key)</span></span></code></pre></div><p>This produces a stable public identifier. It is independent of <code>monad_name</code>, port, host, IP, and endpoint. A monad may move from <code>8161</code> to <code>8162</code>, or from a laptop to a relay, without changing identity if it keeps the same private key.</p><p>The <code>monad_name</code> is a human-readable label chosen by the namespace holder or runtime operator (e.g., <code>&quot;monadlisa&quot;</code>, <code>&quot;worker-a&quot;</code>, <code>&quot;vision-daemon&quot;</code>). Physical placement such as <code>&quot;iphone&quot;</code>, <code>&quot;macbook&quot;</code>, <code>&quot;raspberry-cancun&quot;</code>, or <code>&quot;vm-prod&quot;</code> is metadata resolved by NetGet. It is not the semantic selector.</p><p>The monad proves continuity by publishing <code>cleaker(monad)</code>:</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;monadId&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;monad:&lt;sha256-public-key&gt;&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;monadName&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;haiku&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;cleaker&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;protocol&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;cleaker(monad)&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;version&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;subject&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;monad&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;id&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;monad:&lt;sha256-public-key&gt;&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;publicKey&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;type&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;spki&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;format&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;pem&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;key&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;-----BEGIN PUBLIC KEY-----...&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    },</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;signature&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;algorithm&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;ed25519&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;message&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;{...canonical surface proof...}&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;value&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;&lt;base64url-signature&gt;&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;issuedAt&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1770000000000</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>The resolver verifies:</p><div class="language-txt vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">txt</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>sha256(publicKey) == monad.id</span></span>
<span class="line"><span>verify(publicKey, signature.message, signature.value) == true</span></span></code></pre></div><p>The current implementation publishes this self-signed surface proof. A future challenge form may add resolver-provided nonces for stronger replay resistance.</p><h3 id="_3-2-monad-index-structure" tabindex="-1">3.2 monad index structure <a class="header-anchor" href="#_3-2-monad-index-structure" aria-label="Permalink to &quot;3.2 monad index structure&quot;">​</a></h3><p>The monad index is a <code>.me</code> secret space:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">me.monad[</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;_&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">](</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;namespace_key&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// For each known monad:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">me.monad[monad_id].</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">name</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;monadlisa&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">me.monad[monad_id].</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">endpoint</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;encrypted_endpoint_descriptor&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">me.monad[monad_id].</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">namespace</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;jabellae.cleaker.me&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">me.monad[monad_id].</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">status</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;online&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">me.monad[monad_id].</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">activated_by</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;jabellae.cleaker.me&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">me.monad[monad_id].budget.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cpu</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0.25</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">me.monad[monad_id].</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">capabilities</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">([</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;filesystem&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;camera&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">me.monad[monad_id].placement.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">provider</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;netget&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">me.monad[monad_id].placement.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">target</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;iphone&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">me.monad[monad_id].</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">last_seen</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(timestamp);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">me.monad[monad_id].</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">public_key</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;monad_public_key&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">me.monad[monad_id].</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cleaker</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;latest_signed_surface_proof&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span></code></pre></div><p>The index is stored locally on every authorized monad that holds the namespace key. There is no canonical remote copy. monads sync the index through the mesh on reconnection (see Section 7).</p><p>The <code>endpoint_descriptor</code> field is doubly encrypted: first under the namespace key (so only namespace members can read it), and second it may use an ephemeral monad key for transport security. The endpoint is volatile and may change. The <code>monad_id</code> is stable.</p><h3 id="_3-3-reading-from-the-monad-index" tabindex="-1">3.3 Reading from the monad index <a class="header-anchor" href="#_3-3-reading-from-the-monad-index" aria-label="Permalink to &quot;3.3 Reading from the monad index&quot;">​</a></h3><p>A monad resolves <code>me://jabellae.cleaker.me[monadlisa]/some.path</code> as follows:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>1. Compute: target_id = HMAC-SHA256(namespace_key, &quot;monad:monadlisa&quot;)</span></span>
<span class="line"><span>2. Read: endpoint = me(&quot;monad.&quot; + target_id + &quot;.endpoint&quot;)</span></span>
<span class="line"><span>3. If endpoint is undefined → monad not found → fail with NRP_ERROR_monad_NOT_FOUND</span></span>
<span class="line"><span>4. Decrypt endpoint descriptor → get transport address</span></span>
<span class="line"><span>5. Proceed to Phase 2 (semantic resolution)</span></span></code></pre></div><p>Compatibility bindings may still accept <code>[surface:name]</code> and derive <code>HMAC-SHA256(namespace_key, &quot;surface:&quot; + name)</code>. New implementations should prefer monad selectors.</p><hr><h2 id="_4-the-claim-ceremony" tabindex="-1">4. The Claim Ceremony <a class="header-anchor" href="#_4-the-claim-ceremony" aria-label="Permalink to &quot;4. The Claim Ceremony&quot;">​</a></h2><p>The claim ceremony is how a new monad joins a namespace. It replaces the need for a central authority to &quot;add&quot; a runtime.</p><p>The current implementation has a claim token as a temporary in-memory value. This section formalizes and hardens it.</p><h3 id="_4-1-token-generation" tabindex="-1">4.1 Token generation <a class="header-anchor" href="#_4-1-token-generation" aria-label="Permalink to &quot;4.1 Token generation&quot;">​</a></h3><p>An existing monad in the namespace (the <strong>inviting monad</strong>) generates a claim token:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>nonce         = random_bytes(16)</span></span>
<span class="line"><span>expiry        = now() + TTL            // recommended TTL: 300 seconds</span></span>
<span class="line"><span>claim_token   = HMAC-SHA256(namespace_key, nonce + expiry)</span></span>
<span class="line"><span>claim_payload = base64url(nonce + expiry + claim_token)</span></span></code></pre></div><p>The claim payload is encoded into the URI:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>me://jabellae.cleaker.me[claim:CLAIM_PAYLOAD]/new-monad</span></span></code></pre></div><p>And optionally rendered as a QR code for physical proximity pairing (already implemented in Cleaker).</p><h3 id="_4-2-token-verification" tabindex="-1">4.2 Token verification <a class="header-anchor" href="#_4-2-token-verification" aria-label="Permalink to &quot;4.2 Token verification&quot;">​</a></h3><p>The <strong>new monad</strong> presents the claim payload. The inviting monad (or any namespace monad that receives the pairing request) verifies:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>1. Decode claim_payload → extract nonce, expiry, presented_token</span></span>
<span class="line"><span>2. Check: now() &lt; expiry → if expired, reject with NRP_ERROR_CLAIM_EXPIRED</span></span>
<span class="line"><span>3. Recompute: expected_token = HMAC-SHA256(namespace_key, nonce + expiry)</span></span>
<span class="line"><span>4. Check: presented_token == expected_token → if not, reject with NRP_ERROR_CLAIM_INVALID</span></span>
<span class="line"><span>5. Check: token has not been used before (nonce registry, local) → if used, reject with NRP_ERROR_CLAIM_CONSUMED</span></span></code></pre></div><h3 id="_4-3-key-handshake" tabindex="-1">4.3 Key handshake <a class="header-anchor" href="#_4-3-key-handshake" aria-label="Permalink to &quot;4.3 Key handshake&quot;">​</a></h3><p>After verification, the inviting monad and new monad perform a key handshake:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>1. New monad generates an ephemeral keypair: (eph_pub, eph_priv)</span></span>
<span class="line"><span>2. New monad sends eph_pub to inviting monad</span></span>
<span class="line"><span>3. Inviting monad encrypts namespace_key with eph_pub:</span></span>
<span class="line"><span>     encrypted_namespace_key = ECIES(eph_pub, namespace_key)</span></span>
<span class="line"><span>4. Inviting monad sends encrypted_namespace_key + its own monad_id + current monad index snapshot</span></span>
<span class="line"><span>5. New monad decrypts with eph_priv → receives namespace_key and monad index</span></span>
<span class="line"><span>6. New monad registers itself in the monad index:</span></span>
<span class="line"><span>     monad_id_new = HMAC-SHA256(namespace_key, &quot;monad:&quot; + new_monad_name)</span></span>
<span class="line"><span>     me.monad[monad_id_new].endpoint(...)</span></span>
<span class="line"><span>     me.monad[monad_id_new].public_key(eph_pub)</span></span>
<span class="line"><span>7. New monad announces its registration to all online monads in the namespace</span></span></code></pre></div><h3 id="_4-4-token-consumption" tabindex="-1">4.4 Token consumption <a class="header-anchor" href="#_4-4-token-consumption" aria-label="Permalink to &quot;4.4 Token consumption&quot;">​</a></h3><p>The nonce is added to a local consumed-nonce registry after a successful claim. The registry is pruned of entries older than the maximum TTL. This prevents replay attacks.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">me.system.consumed_nonces[nonce].</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">at</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(timestamp);</span></span></code></pre></div><hr><h2 id="_5-resolution-rules" tabindex="-1">5. Resolution Rules <a class="header-anchor" href="#_5-resolution-rules" aria-label="Permalink to &quot;5. Resolution Rules&quot;">​</a></h2><p>The canonical <code>me://</code> URI names a semantic path. monads are selected internally unless the caller explicitly asks for a technical override.</p><h3 id="_5-0-no-selector-—-canonical-semantic-resolution" tabindex="-1">5.0 No selector — canonical semantic resolution <a class="header-anchor" href="#_5-0-no-selector-—-canonical-semantic-resolution" aria-label="Permalink to &quot;5.0 No selector — canonical semantic resolution&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>me://jabellae.cleaker.me/wallet.balance</span></span></code></pre></div><p>When no selector is present, resolution is <strong>semantic-first</strong>. This is the public form applications and users should prefer.</p><p>The resolver starts from the current/local monad and may then resolve intelligently through the namespace. It may use:</p><ul><li>the current monad</li><li>a specific better-positioned monad</li><li>several monads internally</li><li>the Total monad synthesis</li><li>NetGet endpoint placement</li></ul><p>All of those are execution details. They must not change the meaning of <code>jabellae.cleaker.me/wallet.balance</code>.</p><p>The default resolver is free to use synthesis, routing intelligence, endpoint placement, or any internal strategy without exposing it to the user.</p><h3 id="_5-1-current-—-technical-local-override" tabindex="-1">5.1 <code>[current]</code> — technical local override <a class="header-anchor" href="#_5-1-current-—-technical-local-override" aria-label="Permalink to &quot;5.1 \`[current]\` — technical local override&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>me://jabellae.cleaker.me[current]/profile.name</span></span>
<span class="line"><span>// or equivalently:</span></span>
<span class="line"><span>me://profile.name</span></span></code></pre></div><p>Forces resolution on the monad receiving the request. No topological resolution is needed. Phase 1 is a no-op.</p><h3 id="_5-2-monad-name-—-technical-monad-route-override" tabindex="-1">5.2 <code>[monad-name]</code> — technical monad route override <a class="header-anchor" href="#_5-2-monad-name-—-technical-monad-route-override" aria-label="Permalink to &quot;5.2 \`[monad-name]\` — technical monad route override&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>me://jabellae.cleaker.me[monadlisa]/profile</span></span></code></pre></div><ol><li>Derive <code>target_id = HMAC-SHA256(namespace_key, &quot;monad:monadlisa&quot;)</code></li><li>Look up endpoint in monad index</li><li>If not found: <code>NRP_ERROR_monad_NOT_FOUND</code></li><li>If found but unreachable: <code>NRP_ERROR_monad_UNREACHABLE</code></li><li>If found and reachable: proceed to semantic resolution</li></ol><p>This is for diagnostics, debugging, replay, or advanced routing. It changes execution, not meaning.</p><p>These two URIs identify the same semantic target:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>me://jabellae.cleaker.me[monadlisa]/profile</span></span>
<span class="line"><span>me://jabellae.cleaker.me[monadluis]/profile</span></span></code></pre></div><p>Both target:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>jabellae.cleaker.me/profile</span></span></code></pre></div><p>The selector only decides which monad is asked.</p><h3 id="_5-3-monad-a-monad-b-—-technical-monad-set-override" tabindex="-1">5.3 <code>[monad-a,monad-b]</code> — technical monad set override <a class="header-anchor" href="#_5-3-monad-a-monad-b-—-technical-monad-set-override" aria-label="Permalink to &quot;5.3 \`[monad-a,monad-b]\` — technical monad set override&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>me://jabellae.cleaker.me[monadlisa,worker-a]/tasks/next</span></span></code></pre></div><p>An explicit set selector constrains execution to a bounded group of monads. The mesh may compare hashes, require quorum, or use another policy, but it must still resolve the same namespace/path target. Reads should prefer one coherent disclosure envelope unless the caller explicitly requests a multi-response diagnostic.</p><h3 id="_5-4-mesh-control-plane-is-a-path-not-a-selector" tabindex="-1">5.4 Mesh control plane is a path, not a selector <a class="header-anchor" href="#_5-4-mesh-control-plane-is-a-path-not-a-selector" aria-label="Permalink to &quot;5.4 Mesh control plane is a path, not a selector&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>me://jabellae.cleaker.me/.mesh/monads</span></span>
<span class="line"><span>me://jabellae.cleaker.me/.mesh/monads/monadlisa/status</span></span>
<span class="line"><span>me://jabellae.cleaker.me/.mesh/monads/monadlisa/endpoint</span></span></code></pre></div><p>monad registry, budget, placement, capability, and endpoint data live in the namespace tree as control paths. They are not selector semantics.</p><p>This keeps the selector model pure:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>namespace/path              -&gt; meaning</span></span>
<span class="line"><span>namespace[monadlisa]/path   -&gt; same meaning, forced execution route</span></span>
<span class="line"><span>namespace/.mesh/monads      -&gt; control/introspection path</span></span></code></pre></div><p>The <code>.mesh</code> branch is sensitive and should be treated as a secret/control space by default.</p><h3 id="_5-5-—-compatibility-alias-for-default-synthesis" tabindex="-1">5.5 <code>[]</code> — compatibility alias for default synthesis <a class="header-anchor" href="#_5-5-—-compatibility-alias-for-default-synthesis" aria-label="Permalink to &quot;5.5 \`[]\` — compatibility alias for default synthesis&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>me://jabellae.cleaker.me[]/profile</span></span></code></pre></div><p><code>[]</code> may be accepted as a compatibility or diagnostic spelling of default synthesis, but it is not the canonical user-facing form.</p><p>These should resolve to the same semantic target:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>me://jabellae.cleaker.me/profile</span></span>
<span class="line"><span>me://jabellae.cleaker.me[]/profile</span></span></code></pre></div><p>The preferred public form is <code>namespace/path</code>.</p><h3 id="_5-6-claim-token-—-pairing-handshake" tabindex="-1">5.6 <code>[claim:token]</code> — pairing handshake <a class="header-anchor" href="#_5-6-claim-token-—-pairing-handshake" aria-label="Permalink to &quot;5.6 \`[claim:token]\` — pairing handshake&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>me://jabellae.cleaker.me[claim:CLAIM_PAYLOAD]/new-monad</span></span></code></pre></div><p>Triggers the claim ceremony (Section 4). Does not proceed to semantic resolution — the claim selector is a control-plane operation, not a data-plane operation.</p><h3 id="_5-7-surface-name-—-compatibility-selector" tabindex="-1">5.7 <code>[surface:name]</code> — compatibility selector <a class="header-anchor" href="#_5-7-surface-name-—-compatibility-selector" aria-label="Permalink to &quot;5.7 \`[surface:name]\` — compatibility selector&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>me://jabellae.cleaker.me[surface:iphone]/runtime/battery</span></span></code></pre></div><p><code>[surface:name]</code> is a compatibility selector from earlier drafts. New implementations should model runtime agents as monads and prefer <code>[monad-name]</code>. A compatibility layer may map <code>[surface:name]</code> to a monad selector or derive the old <code>surface_id</code>.</p><hr><h2 id="_6-the-disclosure-model" tabindex="-1">6. The Disclosure Model <a class="header-anchor" href="#_6-the-disclosure-model" aria-label="Permalink to &quot;6. The Disclosure Model&quot;">​</a></h2><p>This is what a target monad returns for each category of path request. This model must be implemented consistently across all monads. A monad that leaks structural information in error responses breaks the security model.</p><h3 id="_6-1-public-path" tabindex="-1">6.1 Public path <a class="header-anchor" href="#_6-1-public-path" aria-label="Permalink to &quot;6.1 Public path&quot;">​</a></h3><p>The path exists and is not within any secret scope. <strong>Returns:</strong> the resolved value.</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;status&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;ok&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;path&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;profile.name&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;value&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;José Abella&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;origin&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;public&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><h3 id="_6-2-stealth-root" tabindex="-1">6.2 Stealth root <a class="header-anchor" href="#_6-2-stealth-root" aria-label="Permalink to &quot;6.2 Stealth root&quot;">​</a></h3><p>The path points to the root of a secret scope (e.g., <code>wallet</code> when <code>wallet</code> is declared with <code>[&quot;_&quot;]</code>). <strong>Returns:</strong> <code>undefined</code>. Explicitly. Not a 404, not an error, not a &quot;path not found.&quot; The absence is honest and intentional.</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;status&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;ok&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;path&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;wallet&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;value&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;origin&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;stealth&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>The distinction between <code>null</code> (stealth root) and <code>NRP_ERROR_PATH_NOT_FOUND</code> (path does not exist at all) is architectural. Callers must not be able to distinguish &quot;this path is secret&quot; from &quot;this path does not exist&quot; — both return a form of <code>undefined</code>. The implementation choice of <code>null</code> vs. omitting the field is left to the transport binding, but the semantics must be indistinguishable to an observer without the secret key.</p><h3 id="_6-3-secret-leaf-—-correct-key" tabindex="-1">6.3 Secret leaf — correct key <a class="header-anchor" href="#_6-3-secret-leaf-—-correct-key" aria-label="Permalink to &quot;6.3 Secret leaf — correct key&quot;">​</a></h3><p>The path is within a secret scope, and the requesting monad presents the correct key (via the <code>secret:key@</code> prefix in the URI, or through a pre-established shared secret).</p><p><strong>Returns:</strong> the resolved value.</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;status&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;ok&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;path&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;wallet.balance&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;value&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">12480</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;origin&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;stealth&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><h3 id="_6-4-secret-leaf-—-wrong-or-absent-key" tabindex="-1">6.4 Secret leaf — wrong or absent key <a class="header-anchor" href="#_6-4-secret-leaf-—-wrong-or-absent-key" aria-label="Permalink to &quot;6.4 Secret leaf — wrong or absent key&quot;">​</a></h3><p>The path is within a secret scope, and the requesting monad does not present the correct key, or presents no key. <strong>Returns:</strong> same envelope as stealth root. The response must be indistinguishable from case 6.2. A wrong key must not produce a different error than no key.</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;status&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;ok&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;path&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;wallet.balance&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;value&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;origin&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;stealth&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>This is the &quot;fail closed, leak nothing&quot; invariant. It directly mirrors the <code>[&quot;_&quot;]</code> axiom in the <code>.me</code> kernel (A0 + A2).</p><h3 id="_6-5-path-does-not-exist" tabindex="-1">6.5 Path does not exist <a class="header-anchor" href="#_6-5-path-does-not-exist" aria-label="Permalink to &quot;6.5 Path does not exist&quot;">​</a></h3><p>The path is not declared in the kernel and is not within any secret scope.</p><p><strong>Returns:</strong></p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;status&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;not_found&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;path&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;does.not.exist&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>This is the only case where a <code>not_found</code> status is returned. It must only be returned for genuinely absent paths that are not near any secret scope. If there is any ambiguity about whether a path might be secret, return the stealth envelope (6.2) instead.</p><hr><h2 id="_7-monad-index-synchronization" tabindex="-1">7. monad Index Synchronization <a class="header-anchor" href="#_7-monad-index-synchronization" aria-label="Permalink to &quot;7. monad Index Synchronization&quot;">​</a></h2><p>The monad index is local on each authorized monad. When monads reconnect after being offline, they may have divergent views of the index. The protocol uses a simple Last-Write-Wins (LWW) strategy, consistent with the <code>.me</code> kernel&#39;s A9 axiom (deterministic conflict resolution).</p><h3 id="_7-1-sync-on-reconnect" tabindex="-1">7.1 Sync on reconnect <a class="header-anchor" href="#_7-1-sync-on-reconnect" aria-label="Permalink to &quot;7.1 Sync on reconnect&quot;">​</a></h3><p>When monad A reconnects to monad B:</p><ol><li>A sends its monad index version vector (a map of <code>monad_id → last_seen_timestamp</code>)</li><li>B compares against its own version vector</li><li>Each sends the other the entries that are newer on its side</li><li>Both apply the updates using LWW on <code>last_seen</code> timestamp</li></ol><h3 id="_7-2-monad-expiry" tabindex="-1">7.2 monad expiry <a class="header-anchor" href="#_7-2-monad-expiry" aria-label="Permalink to &quot;7.2 monad expiry&quot;">​</a></h3><p>A monad entry that has not updated its <code>last_seen</code> timestamp within a configurable window (e.g., 30 days) may be marked stale. Stale monads are kept in the index but deprioritized in routing. They are not deleted automatically — deletion requires an explicit <code>[&quot;-&quot;]</code> operation by a namespace holder.</p><h3 id="_7-3-endpoint-volatility" tabindex="-1">7.3 Endpoint volatility <a class="header-anchor" href="#_7-3-endpoint-volatility" aria-label="Permalink to &quot;7.3 Endpoint volatility&quot;">​</a></h3><p>Endpoint descriptors are volatile. A monad&#39;s IP address, NetGet placement, or relay address may change frequently. Each monad is responsible for updating its own endpoint descriptor in the index whenever its transport address changes, and publishing the update to all online monads.</p><hr><h2 id="_8-error-reference" tabindex="-1">8. Error Reference <a class="header-anchor" href="#_8-error-reference" aria-label="Permalink to &quot;8. Error Reference&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Code</th><th>Meaning</th></tr></thead><tbody><tr><td><code>NRP_ERROR_monad_NOT_FOUND</code></td><td><code>monad_id</code> not in local index</td></tr><tr><td><code>NRP_ERROR_monad_UNREACHABLE</code></td><td>monad_id found but endpoint not reachable</td></tr><tr><td><code>NRP_ERROR_SURFACE_NOT_FOUND</code></td><td>Compatibility alias for <code>NRP_ERROR_monad_NOT_FOUND</code></td></tr><tr><td><code>NRP_ERROR_SURFACE_UNREACHABLE</code></td><td>Compatibility alias for <code>NRP_ERROR_monad_UNREACHABLE</code></td></tr><tr><td><code>NRP_ERROR_CLAIM_EXPIRED</code></td><td>claim token TTL exceeded</td></tr><tr><td><code>NRP_ERROR_CLAIM_INVALID</code></td><td>HMAC verification failed</td></tr><tr><td><code>NRP_ERROR_CLAIM_CONSUMED</code></td><td>nonce already used</td></tr><tr><td><code>NRP_ERROR_NAMESPACE_UNKNOWN</code></td><td>no namespace key available for this namespace</td></tr><tr><td><code>NRP_ERROR_PATH_NOT_FOUND</code></td><td>path does not exist and is not near any secret scope</td></tr><tr><td><code>NRP_ERROR_TRANSPORT</code></td><td>network-level failure (connection refused, timeout, etc.)</td></tr></tbody></table><hr><h2 id="_9-what-is-not-specified-here-draft-scope" tabindex="-1">9. What Is Not Specified Here (draft scope) <a class="header-anchor" href="#_9-what-is-not-specified-here-draft-scope" aria-label="Permalink to &quot;9. What Is Not Specified Here (draft scope)&quot;">​</a></h2><p>The following are intentionally deferred to later versions: <strong>Transport binding</strong> — This document specifies the protocol semantics, not the wire format. A future transport binding document will specify the exact message encoding (JSON-over-WebSocket, protobuf, etc.) and the TLS/noise handshake for monad-to-monad connections. <strong>Relay infrastructure</strong> — When two monads cannot reach each other directly (NAT, firewall, different networks), a relay is needed. The relay protocol is not specified here. The relay must not be able to read message content — it is a blind forwarder. Relay discovery and selection are deferred. <strong>NetGet placement binding</strong> — NetGet can resolve physical placement such as iPhone, Raspberry Pi, VM, or localhost into endpoint descriptors. This document specifies the semantic role of those descriptors, not the NetGet deployment API. <strong>Namespace key distribution across monads</strong> — This document assumes the namespace key is already present on the requesting monad. The initial key bootstrap (how the first monad gets the namespace key from the derivation seed) is specified by the <code>.me</code> kernel, not the NRP. Subsequent key sharing is handled by the claim ceremony (Section 4). <strong>Multi-namespace resolution</strong> — A monad may hold keys for multiple namespaces. How a monad selects the correct namespace key when a URI omits the namespace qualifier is implementation-defined in this draft. <strong>Revocation</strong> — How to revoke a monad from a namespace (e.g., a lost device or compromised runtime) without a central authority is an open problem. A candidate mechanism is a signed revocation entry in the monad index, propagated to all monads. Deferred to a future version.</p><hr><h2 id="_10-design-principles" tabindex="-1">10. Design Principles <a class="header-anchor" href="#_10-design-principles" aria-label="Permalink to &quot;10. Design Principles&quot;">​</a></h2><p>These are the invariants that any implementation must preserve. They are not implementation details — they are the protocol&#39;s soul. <strong>1. No central registry.</strong> The monad index is local to each namespace holder. Discovery requires holding the namespace key. There is nothing to hack that reveals a complete list of monads or identities. <strong>2. Identity is computation, not storage.</strong> A monad&#39;s identity is derived from a key and a name. It is not assigned by an authority and not stored in a global directory. <strong>3. Honest absence.</strong> A stealth root returns <code>undefined</code>, not an error. The protocol does not reveal the existence of secret scopes to parties without the key. Absence and secrecy are indistinguishable from the outside. <strong>4. Fail closed.</strong> Wrong keys produce the same response as absent keys. The protocol leaks no information about why a resolution returned <code>undefined</code>. <strong>5. Minimum exposure.</strong> monads exchange only what is necessary. Default resolution sends path read requests and reconciliation material — not the full kernel state, not the full monad index, not any metadata beyond what the request requires. <strong>6. Mathematical fragmentation.</strong> No single monad must hold a complete picture of any namespace. The identity is distributed across monads by design. Stealing one monad does not compromise the namespace. <strong>7. Offline-capable.</strong> Local resolution (<code>[current]</code> or no selector) works with no network access. The protocol degrades gracefully: offline monads are skipped or deprioritized, missing monads return <code>NRP_ERROR_monad_UNREACHABLE</code>. The local kernel is never blocked by network state. <strong>8. The namespace is a semantic filesystem.</strong> The namespace is not a server, not an endpoint, not an account. It is the living, distributed, encrypted graph of who you are, what you have, and what the mesh can execute or deliver on your behalf. Paths in the namespace behave like paths in a filesystem — with mounts, secret scopes, executable handlers, and remote resources. The mesh is the transport layer that makes this filesystem accessible from anywhere, like NFS or IPFS — but semantic, encrypted, and under sovereign control. <strong>9. monads are invisible execution routes.</strong> The canonical address is <code>namespace/path</code>. <code>namespace[monadA]/path</code> and <code>namespace[monadB]/path</code> are technical overrides that point to the same semantic node through different execution routes. <strong>10. Default resolution is synthesis, not a list.</strong> <code>namespace/path</code> returns one coherent disclosure envelope. Parallel querying, quorum, latest-hash selection, and authority policy are internal strategies.</p><p><em>Meaning lives in the namespace. Execution lives in the mesh.</em></p><hr><h2 id="appendix-a-—-formal-grammar-abnf-semantic-path-draft" tabindex="-1">Appendix A — Formal Grammar (ABNF, semantic path draft) <a class="header-anchor" href="#appendix-a-—-formal-grammar-abnf-semantic-path-draft" aria-label="Permalink to &quot;Appendix A — Formal Grammar (ABNF, semantic path draft)&quot;">​</a></h2><p>The canonical user-facing grammar is semantic:</p><div class="language-abnf vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">abnf</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>me-uri       = &quot;me://&quot; [ namespace ] [ &quot;/&quot; path ]</span></span>
<span class="line"><span></span></span>
<span class="line"><span>namespace    = 1*( ALPHA / DIGIT / &quot;.&quot; / &quot;_&quot; / &quot;-&quot; )</span></span>
<span class="line"><span>path         = *( VCHAR / &quot;/&quot; )</span></span></code></pre></div><p>A selector exists only in the advanced grammar and should be omitted in normal user-facing addresses.</p><p>Advanced technical selectors may constrain execution without changing meaning:</p><div class="language-abnf vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">abnf</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>advanced-me-uri = &quot;me://&quot; [ namespace ] selector [ &quot;/&quot; path ]</span></span>
<span class="line"><span>selector       = &quot;[&quot; ( &quot;current&quot; / monad-list / empty-selector / &quot;claim:&quot; token / legacy-surface ) &quot;]&quot;</span></span>
<span class="line"><span>monad-list     = monad-name *( &quot;,&quot; monad-name )</span></span>
<span class="line"><span>monad-name     = 1*( ALPHA / DIGIT / &quot;-&quot; / &quot;_&quot; )</span></span>
<span class="line"><span>empty-selector = &quot;&quot;</span></span>
<span class="line"><span>legacy-surface = &quot;surface:&quot; monad-name</span></span>
<span class="line"><span>token          = 1*( ALPHA / DIGIT / &quot;-&quot; / &quot;_&quot; )</span></span></code></pre></div><p>Control-plane and introspection data are ordinary paths, not selectors:</p><div class="language-abnf vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">abnf</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>mesh-path      = &quot;.mesh&quot; [ &quot;/&quot; mesh-subpath ]</span></span>
<span class="line"><span>mesh-subpath   = *( VCHAR / &quot;/&quot; )</span></span></code></pre></div><p>The <code>secret:key@namespace</code> prefix for authenticated access is parsed as:</p><div class="language-abnf vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">abnf</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>me-uri-auth  = &quot;me://&quot; &quot;secret:&quot; secret-key &quot;@&quot; namespace [ selector ] [ &quot;/&quot; path ]</span></span>
<span class="line"><span>secret-key   = 1*( ALPHA / DIGIT / &quot;-&quot; / &quot;_&quot; )</span></span></code></pre></div><hr><h2 id="appendix-b-—-current-http-binding-compatibility-layer" tabindex="-1">Appendix B — Current HTTP Binding (Compatibility Layer) <a class="header-anchor" href="#appendix-b-—-current-http-binding-compatibility-layer" aria-label="Permalink to &quot;Appendix B — Current HTTP Binding (Compatibility Layer)&quot;">​</a></h2><p>The canonical resource grammar remains the one defined in this document:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>me://namespace/path</span></span></code></pre></div><p>The current <code>monad.ai</code> server already supports a primary HTTP binding in which the namespace is resolved from the <code>Host</code> header and the daemon remains local/offline-first as a monad runtime.</p><h3 id="primary-http-binding" tabindex="-1">Primary HTTP binding <a class="header-anchor" href="#primary-http-binding" aria-label="Permalink to &quot;Primary HTTP binding&quot;">​</a></h3><ul><li><code>GET /&lt;path&gt;</code> with <code>Host: &lt;namespace&gt;</code> — read from the namespace resolved from <code>Host</code></li><li><code>POST /</code> with <code>Host: &lt;namespace&gt;</code> and <code>{&quot;operation&quot;:&quot;write&quot;, &quot;expression&quot;:&quot;...&quot;, &quot;value&quot;:..., &quot;identityHash&quot;:&quot;...&quot;}</code> — write into the namespace resolved from <code>Host</code></li><li><code>POST /</code> with <code>Host: &lt;namespace&gt;</code> and <code>{&quot;operation&quot;:&quot;claim&quot;, &quot;secret&quot;:&quot;...&quot;, &quot;proof&quot;:{...}}</code> — perform a claim against the namespace resolved from <code>Host</code></li><li><code>POST /</code> with <code>Host: &lt;namespace&gt;</code> and <code>{&quot;operation&quot;:&quot;open&quot;, &quot;secret&quot;:&quot;...&quot;, &quot;identityHash&quot;:&quot;...&quot;}</code> — reopen the namespace resolved from <code>Host</code></li></ul><p>In this binding, the transport endpoint may still be local, such as <code>http://localhost:8161</code>, while the semantic namespace is selected through <code>Host</code>. This is consistent with a local/offline-first monad acting as the material runtime for a semantic namespace.</p><h3 id="current-implementation-notes" tabindex="-1">Current implementation notes <a class="header-anchor" href="#current-implementation-notes" aria-label="Permalink to &quot;Current implementation notes&quot;">​</a></h3><ul><li>Read uses the path in the URL, while write currently uses the <code>expression</code> field in the request body. These refer to the same semantic selector, but they are serialized differently in each direction in the current implementation.</li><li>Write authorization on a claimed namespace is enforced. In the current implementation, a write is accepted when the request body <code>identityHash</code> matches the stored claim identity hash, or when the request carries a valid signature verifiable against the claim&#39;s stored public key.</li><li>Claim currently accepts a hybrid proof model: <code>proof</code> (Ed25519 signature) proves identity, while <code>secret</code> still seeds the unlock state. Open currently remains anchored to <code>identityHash</code>, while <code>secret</code> acts as an unlock key for reopening the namespace state.</li></ul><h3 id="deprecated-compatibility-routes" tabindex="-1">Deprecated / compatibility routes <a class="header-anchor" href="#deprecated-compatibility-routes" aria-label="Permalink to &quot;Deprecated / compatibility routes&quot;">​</a></h3><p>The current server implementation may also expose overlapping compatibility routes in addition to the primary binding. In particular, the current <code>monad.ai</code> server exposes:</p><ul><li><code>POST /claims</code> — a profile-oriented claim bootstrap endpoint. In the current implementation it expects <code>namespace</code>, <code>secret</code>, <code>identityHash</code>, and profile fields such as <code>username</code>, <code>name</code>, <code>email</code>, and <code>phone</code>.</li><li><code>POST /claims/open</code> — an HTTP endpoint for reopening a claimed namespace and recovering its state. In the current implementation it expects <code>namespace</code>, <code>secret</code>, and <code>identityHash</code>.</li><li><code>POST /me/kernel:claim/&lt;namespace&gt;</code> and <code>POST /me/kernel:open/&lt;namespace&gt;</code> — bridge routes that expose kernel command targets over HTTP.</li></ul><p>These routes are part of the current implementation and compatibility layer. They do not redefine the canonical <code>me://</code> resource grammar described by the NRP, and they should not be treated as the primary expression of the protocol.</p><p>These bindings also only partially overlap. They may validate different request bodies, return different status codes, or expose different behavior during bootstrap and testing. They should not be treated as equivalent transport expressions of the protocol.</p><p>A future transport binding document may replace or consolidate these routes, but any such wire-level evolution must preserve the canonical resource identity described here.</p><hr><h2 id="appendix-c-—-open-questions-for-v0-2" tabindex="-1">Appendix C — Open Questions for v0.2 <a class="header-anchor" href="#appendix-c-—-open-questions-for-v0-2" aria-label="Permalink to &quot;Appendix C — Open Questions for v0.2&quot;">​</a></h2><p>These are the questions this document intentionally leaves open, in priority order:</p><ol><li><strong>Relay protocol</strong> — How do two monads on different networks find a common relay, and how does the relay stay blind?</li><li><strong>monad revocation</strong> — How does a namespace holder revoke a lost or compromised monad without a central authority?</li><li><strong>Namespace discovery</strong> — Can a monad discover other namespaces it does not already hold keys for? (Current answer: no. Is this the right answer forever?)</li><li><strong>Index consistency under partition</strong> — If the mesh is partitioned for a long time and monads update their own entries, what is the merge strategy beyond LWW?</li><li><strong>Derivation-based namespace bootstrap</strong> — Should the namespace key be derivable from a BIP-39 mnemonic or similar standard seed phrase, so it can be reconstructed without any network access?</li><li><strong>Cross-namespace pointers</strong> — The <code>[&quot;-&gt;&quot;]</code> operator in <code>.me</code> can create semantic links between namespaces. How does the NRP resolve a path that crosses namespace boundaries?</li></ol><hr><p><strong>∴ Witness our seal</strong><strong>suiGn / neurons.me</strong><strong>v0.2.0-draft</strong></p>`,223)])])}const u=a(i,[["render",o]]);export{m as __pageData,u as default};
